name: Web server

on:
  push:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest
    # environment: mrlectus

    strategy:
      matrix:
        node-version: [22.x]
    steps:
    - name: do checkout
      uses: actions/checkout@v4
    - name: ssh into server
      uses: shimataro/ssh-key-action@v2
      with:
        key: ${{ secrets.SSH_KEY }}
        known_hosts: "unnecessary"
    - name: known host
      run: ssh-keyscan -H ${{ secrets.PHOST }} >> ~/.ssh/known_hosts
    - run: rsync -avz . 'root@${{secrets.PHOST}}':/root/deploy/ --exclude={node_modules,.git}
    - run: echo '${{ vars.ENV }}' > .env
    - run: cat .env
    - run: rsync -avz .env 'root@${{secrets.PHOST}}':/root/deploy/
    - name: install dependencies
      run: |
        ssh 'root@${{ secrets.PHOST }}' "cd deploy; ~/.bun/bin/bun install; pm2 start --interpreter ~/.bun/bin/bun src/index.ts --name server"
